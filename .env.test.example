# Agent Registration & Test Configuration
# Copy this to .env.test and fill in your values

# Required: Your Arke USER API key (for registration, NOT the agent key)
# This is the key you use to log into Arke, starting with "uk_"
ARKE_USER_KEY=uk_your_user_api_key_here

# Optional: Override API URL (default: https://arke-v1.arke.institute)
# ARKE_API_URL=https://arke-v1.arke.institute

# Optional: Override agent endpoint (default: from agent.json)
# AGENT_ENDPOINT=https://your-agent.workers.dev

# Optional: Specify existing agent home collection
# If not set, the first agent registered will create one and share it via agents.registry.json
# AGENT_HOME=IIXXXXXXXXXX
